# Plan

Install canonical workflows. Compare exact bytes, validate syntax and gate routing tests, run existing CI, merge the trusted-base installation under explicit owner authorization, then observe the exact-head disposition in GitHub. This workflow-only installation changes no running application bundle.

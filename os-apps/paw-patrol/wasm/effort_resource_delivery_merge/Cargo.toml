[package]
name = "effort-resource-delivery-merge"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[workspace]

[dependencies]
effort-resource-delivery = { path = "../effort_resource_delivery" }
